Name:
Email:
PID:

================================================================================
   Task 2
================================================================================
INSTRUCTIONS:
1. Do NOT modify the question numbers (e.g., Question 1, Question 2).
2. Type your answers directly below the dashed line for each question.
3. Save this file as 'submission.txt' and upload it to Gradescope.
4. For the following questions, assume that any underlying dynamic arrays
have sufficient initial capacity, or that capacity expansion is handled
identically across data structures and can be ignored for your time
complexity comparisons.
================================================================================

Question 1: A developer implements a Stack ADT using a standard ArrayList
instead of a Deque. For the push(element) operation, they use
list.add(element). For the pop() operation, they use list.remove(list.size()
- 1).

A) What is the worst-case Big-O time complexity of executing a sequence of N
consecutive push operations, followed immediately by N consecutive pop
operations?

B) Explain exactly what happens in the underlying array during the pop
sequence to justify your answer for Part A.

Max 400 characters.
--------------------------------------------------------------------------------

[[Delete this line and type your answer here]]

================================================================================
Question 2: A developer implements a Queue ADT using a standard ArrayList
instead of a deuque. For the enqueue(element) operation, they use
list.add(element). For the dequeue() operation, they use list.remove(0).

A) What is the worst-case Big-O time complexity of executing a sequence of N
consecutive enqueue operations, followed immediately by N consecutive dequeue
operations?

B) Explain exactly what happens in the underlying array during the dequeue
sequence to justify your answer for Part A, and why this differs fundamentally
from the Stack implementation above.

Max 400 characters.
--------------------------------------------------------------------------------

[[Delete this line and type your answer here]]

================================================================================
   END OF SUBMISSION
================================================================================
